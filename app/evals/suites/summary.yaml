name: summary
role: chat
description: Short, non-empty summary.
cases:
  - name: one_sentence
    prompt: |
      Summarize in a single sentence: the sky looks blue because the atmosphere
      scatters short-wavelength light more strongly.
    checks:
      - non_empty
      - max_length:600
